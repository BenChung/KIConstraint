[project]
name = "kiconstraint"
version = "0.1.0"
description = "KiCAD constraint management plugin"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["plugin/plugins"]
